/**
 * PLACEHOLDER UTILITY FUNCTIONS — KEEP OR REMOVE
 *
 * Note for developers and LLMs:
 * This file contains sample/placeholder utility functions used for tests and demos.
 * Once real utility functions are needed, remove this file OR rename it and
 * rewrite its contents completely to reflect real utility functions.
 *
 * - Remove if not needed.
 * - Or: Rename + fully reimplement before production use.
 */

function add(a, b) {
  return a + b;
}

function tellBirthday(person) {
  return `${person.introduce()} I was born in ${person.getBirthYear()}.`;
}

module.exports = { add, tellBirthday };
